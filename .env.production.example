# =====================================================
# Server5K - CONFIGURACIÓN DE PRODUCCIÓN
# =====================================================
# ⚠️  CRÍTICO: Este archivo contiene valores de PRODUCCIÓN
# NO subas el archivo .env real a control de versiones
# Copia este archivo a .env y modifica con tus credenciales reales

# =============== SEGURIDAD OBLIGATORIA =================
# SECRET_KEY: Genera una clave de 50+ caracteres
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=COPIA_AQUI_TU_CLAVE_SECRETA_DE_50_CARACTERES_GENERADA

# DEBUG: NUNCA debe ser True en producción
DEBUG=False

# ALLOWED_HOSTS: Dominios reales de tu aplicación (sin espacios)
# Ejemplo: server5k.com,api.server5k.com,www.server5k.com
ALLOWED_HOSTS=midominio.com,api.midominio.com

# =============== BASE DE DATOS (OBLIGATORIO) =================
POSTGRES_DB=server5k_prod
POSTGRES_USER=server5k_prod_user
POSTGRES_PASSWORD=GENERA_UN_PASSWORD_SEGURO_DE_32_CARACTERES_AQUI
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
# En producción usa 'require' o 'verify-full'
POSTGRES_SSLMODE=require

# =============== REDIS (OBLIGATORIO) =================
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# =============== CORS (OBLIGATORIO) =================
# Orígenes exactos de tu frontend en producción
# NUNCA uses "*" o dominios de desarrollo aquí
CORS_ALLOWED_ORIGINS=https://app.midominio.com,https://admin.midominio.com

# =============== EMAIL (SMTP) =================
# Configura con tu proveedor de email real
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=tu_email_prod@gmail.com
EMAIL_HOST_PASSWORD=tu_password_de_app_gmail
DEFAULT_FROM_EMAIL=noreply@midominio.com

# =============== LOGGING =================
LOG_DIR=/ruta/a/tu/proyecto/logs
